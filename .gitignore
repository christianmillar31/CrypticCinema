
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js build output
/.next/
/out/

# Production
/build

# Static Site Generators
/public # This is often where SSGs store their output

# Miscellaneous
.DS_Store
*.pem

# Node Secret Files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
key.pem
cert.pem

# Local environment variables
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript cache
*.tsbuildinfo

# IDEs and editors
/.idea
/.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# Optional eslint cache
.eslintcache

# Optional Prettier cache
.prettiercache

# Docusaurus output
.docusaurus

# Monorepo Yarn Berry specific
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Firebase
.firebase/
firebase-debug.log
firebase.json # Often user-specific, but can be committed if generic enough
firestore.rules # Commit if you manage rules in code
firestore.indexes.json # Commit if you manage indexes in code
database.rules.json # Commit if you manage rules in code
storage.rules # Commit if you manage rules in code

# Genkit
.genkit/

# Python specific
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
/venv/
/env/
pip-freeze.txt
pipenv.lock

# Movie data file if generated in root (should be in src/data)
/tmdb_movies.json
